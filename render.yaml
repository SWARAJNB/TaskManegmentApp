databases:
  - name: taskflow-db
    plan: free
    databaseName: taskflow_db
    user: taskflow

services:
  - type: web
    name: taskflow-backend
    runtime: python
    plan: starter
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://taskmanagement-frontend-6p1k.onrender.com,https://taskmanagmentapp.onrender.com
      - key: PYTHON_VERSION
        value: "3.11"

  - type: web
    name: taskflow-frontend
    runtime: static
    plan: starter
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://taskmanagmentapp.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
